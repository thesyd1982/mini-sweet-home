#!/bin/bash

# ==========================================
# 🩺 DIAGNOSTIC ENVIRONNEMENT RUST
# ==========================================
# Usage: ./rust-doctor
# Description: Diagnostic complet de l'environnement Rust

set -euo pipefail

# Couleurs
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
NC='\033[0m'

log() { echo -e "${BLUE}[INFO]${NC} $1"; }
success() { echo -e "${GREEN}[✓]${NC} $1"; }
warning() { echo -e "${YELLOW}[⚠]${NC} $1"; }
error() { echo -e "${RED}[✗]${NC} $1"; }
recommend() { echo -e "${CYAN}[💊]${NC} $1"; }

echo "🩺 Diagnostic de l'environnement Rust"
echo "===================================="
echo ""

# Vérifications de base
log "Vérification de l'installation Rust..."

if command -v rustc >/dev/null 2>&1; then
    success "rustc installé: $(rustc --version)"
else
    error "rustc non trouvé"
    recommend "Installez Rust avec: make rust-full"
fi

if command -v cargo >/dev/null 2>&1; then
    success "cargo installé: $(cargo --version)"
else
    error "cargo non trouvé"
    recommend "Installez Rust avec: make rust-full"
fi

if command -v rustup >/dev/null 2>&1; then
    success "rustup installé: $(rustup --version | head -1)"
else
    error "rustup non trouvé"
    recommend "Installez Rust avec: make rust-full"
fi

echo ""

# Vérification des outils Rust
log "Vérification des outils Rust modernes..."

tools=(
    "dust:du-dust:Analyseur d'espace disque"
    "rg:ripgrep:Recherche rapide"
    "fd:fd-find:Alternative à find"
    "bat:bat:Cat avec coloration"
    "exa:exa:Ls moderne"
    "zoxide:zoxide:CD intelligent"
    "starship:starship:Prompt moderne"
)

for tool_info in "${tools[@]}"; do
    IFS=':' read -r cmd package desc <<< "$tool_info"
    if command -v "$cmd" >/dev/null 2>&1; then
        success "$desc ($cmd) installé"
    else
        warning "$desc ($cmd) non trouvé"
        recommend "Installez avec: cargo install $package"
    fi
done

echo ""

# Vérification de l'environnement
log "Vérification de l'environnement..."

if [[ ":$PATH:" == *":$HOME/.cargo/bin:"* ]]; then
    success "~/.cargo/bin dans le PATH"
else
    warning "~/.cargo/bin pas dans le PATH"
    recommend "Ajoutez 'source ~/.cargo/env' à votre ~/.zshrc ou ~/.bashrc"
fi

if [[ -f "$HOME/.cargo/env" ]]; then
    success "Fichier d'environnement Cargo présent"
else
    warning "Fichier ~/.cargo/env manquant"
    recommend "Réinstallez Rust avec rustup"
fi

if [[ -f "$HOME/.cargo/config.toml" ]]; then
    success "Configuration Cargo personnalisée trouvée"
else
    warning "Pas de configuration Cargo personnalisée"
    recommend "Exécutez: make rust-full pour une configuration optimisée"
fi

echo ""

# Vérifications spécifiques WSL
if grep -qi microsoft /proc/version 2>/dev/null; then
    log "Environnement WSL détecté - Vérifications spécifiques..."
    
    if [[ -f "$HOME/.cargo/config.toml" ]] && grep -q "target-dir" "$HOME/.cargo/config.toml"; then
        success "Configuration WSL optimisée (target-dir configuré)"
    else
        warning "Configuration WSL non optimisée"
        recommend "Configurez target-dir pour de meilleures performances"
    fi
    
    # Vérifier Windows Defender exclusions
    if command -v powershell.exe >/dev/null 2>&1; then
        log "Vérification des exclusions Windows Defender..."
        # Note: Cette vérification nécessiterait des droits admin
        warning "Vérifiez manuellement les exclusions Windows Defender pour ~/.cargo"
        recommend "Ajoutez ~/.cargo aux exclusions pour de meilleures performances"
    fi
fi

echo ""

# Vérification des performances
log "Test de performance Cargo..."

if command -v cargo >/dev/null 2>&1; then
    # Test simple de compilation
    temp_dir=$(mktemp -d)
    cd "$temp_dir"
    
    cargo init --name test-perf --quiet 2>/dev/null || true
    
    if [[ -f "Cargo.toml" ]]; then
        echo "Temps de compilation d'un projet Hello World :"
        time cargo build --quiet 2>/dev/null || warning "Échec du test de compilation"
    fi
    
    cd - >/dev/null
    rm -rf "$temp_dir"
fi

echo ""

# Recommandations générales
log "Recommandations générales..."

if ! command -v dust >/dev/null 2>&1; then
    recommend "Installez dust pour un analyseur d'espace disque moderne: make dust"
fi

if ! command -v rg >/dev/null 2>&1; then
    recommend "Installez ripgrep pour des recherches ultra-rapides: cargo install ripgrep"
fi

if ! command -v fd >/dev/null 2>&1; then
    recommend "Installez fd pour remplacer find: cargo install fd-find"
fi

# Vérification des mises à jour
if command -v cargo >/dev/null 2>&1; then
    if ! command -v cargo-install-update >/dev/null 2>&1; then
        recommend "Installez cargo-update pour maintenir vos outils: cargo install cargo-update"
    else
        log "Vérification des mises à jour disponibles..."
        outdated=$(cargo install-update --list 2>/dev/null | grep -c "^[^[:space:]]" || echo "0")
        if [[ "$outdated" -gt 0 ]]; then
            warning "$outdated outils peuvent être mis à jour"
            recommend "Mettez à jour avec: make rust-update"
        else
            success "Tous les outils Rust sont à jour"
        fi
    fi
fi

echo ""

# Résumé et score
log "Résumé du diagnostic..."

score=0
total=10

command -v rustc >/dev/null 2>&1 && ((score++))
command -v cargo >/dev/null 2>&1 && ((score++))
command -v rustup >/dev/null 2>&1 && ((score++))
command -v dust >/dev/null 2>&1 && ((score++))
command -v rg >/dev/null 2>&1 && ((score++))
command -v fd >/dev/null 2>&1 && ((score++))
command -v bat >/dev/null 2>&1 && ((score++))
command -v exa >/dev/null 2>&1 && ((score++))
[[ ":$PATH:" == *":$HOME/.cargo/bin:"* ]] && ((score++))
[[ -f "$HOME/.cargo/config.toml" ]] && ((score++))

percentage=$((score * 100 / total))

echo ""
if [[ $percentage -ge 90 ]]; then
    success "🎉 Environnement Rust excellent ($score/$total - $percentage%)"
elif [[ $percentage -ge 70 ]]; then
    success "✅ Environnement Rust bon ($score/$total - $percentage%)"
elif [[ $percentage -ge 50 ]]; then
    warning "⚠️  Environnement Rust correct ($score/$total - $percentage%)"
else
    error "❌ Environnement Rust à améliorer ($score/$total - $percentage%)"
fi

echo ""

if [[ $percentage -lt 90 ]]; then
    echo "🚀 Actions recommandées pour améliorer votre environnement :"
    
    if [[ $score -lt 3 ]]; then
        echo "   1. Installation complète: make rust-full"
    else
        echo "   1. Installation des outils manquants: make modern"
    fi
    
    echo "   2. Configuration optimisée: Éditez ~/.cargo/config.toml"
    echo "   3. Test des outils: make dust-demo"
    echo "   4. Mise à jour régulière: make rust-update"
fi

echo ""
echo "📚 Documentation complète: ~/mini-sweet-home/docs/rust-installation-guide.md"
