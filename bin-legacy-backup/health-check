#!/bin/bash

# ===============================
# 🔄 MINI SWEET HOME - HEALTH CHECK
# ===============================

# Vérification continue de la santé du système
# Usage: ./bin/health-check [--watch]

# Colors
GREEN='\033[0;32m'
RED='\033[0;31m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

health_check() {
    local timestamp=$(date '+%Y-%m-%d %H:%M:%S')
    echo -e "${BLUE}🔄 Health Check - $timestamp${NC}"
    
    local issues=0
    
    # 1. Vérifier les liens symboliques
    echo -n "🔗 Symlinks... "
    if [[ -L ~/.zshrc && -L ~/.tmux.conf && -L ~/.gitconfig && -L ~/.config/nvim ]]; then
        echo -e "${GREEN}✅${NC}"
    else
        echo -e "${RED}❌${NC}"
        ((issues++))
    fi
    
    # 2. Vérifier les performances ZSH
    echo -n "⚡ ZSH performance... "
    if command -v zsh >/dev/null 2>&1; then
        local zsh_time=$(timeout 5 bash -c 'time zsh -i -c exit' 2>&1 | grep real | awk '{print $2}' | sed 's/[^0-9.]//g' || echo "999")
        if (( $(echo "$zsh_time < 1.0" | bc -l 2>/dev/null || echo 0) )); then
            echo -e "${GREEN}✅ (${zsh_time}s)${NC}"
        else
            echo -e "${YELLOW}⚠️ (${zsh_time}s)${NC}"
        fi
    else
        echo -e "${RED}❌${NC}"
        ((issues++))
    fi
    
    # 3. Vérifier les outils essentiels
    echo -n "🛠️ Essential tools... "
    local missing_tools=0
    for tool in git curl wget; do
        if ! command -v "$tool" >/dev/null 2>&1; then
            ((missing_tools++))
        fi
    done
    
    if [[ $missing_tools -eq 0 ]]; then
        echo -e "${GREEN}✅${NC}"
    else
        echo -e "${YELLOW}⚠️ ($missing_tools missing)${NC}"
    fi
    
    # 4. Vérifier l'espace disque
    echo -n "💾 Disk space... "
    local disk_usage=$(df ~ | tail -1 | awk '{print $5}' | sed 's/%//')
    if [[ $disk_usage -lt 90 ]]; then
        echo -e "${GREEN}✅ (${disk_usage}% used)${NC}"
    else
        echo -e "${YELLOW}⚠️ (${disk_usage}% used)${NC}"
    fi
    
    # 5. Vérifier les processus suspects
    echo -n "🔍 Process health... "
    local zombie_count=$(ps aux | awk '$8 ~ /^Z/ { count++ } END { print count+0 }')
    if [[ $zombie_count -eq 0 ]]; then
        echo -e "${GREEN}✅${NC}"
    else
        echo -e "${YELLOW}⚠️ ($zombie_count zombies)${NC}"
    fi
    
    # Résumé
    if [[ $issues -eq 0 ]]; then
        echo -e "${GREEN}🎉 System healthy!${NC}"
        return 0
    else
        echo -e "${YELLOW}⚠️ $issues issue(s) detected${NC}"
        return 1
    fi
}

# Mode watch
if [[ "$1" == "--watch" ]]; then
    echo "🔄 Starting continuous health monitoring (Ctrl+C to stop)..."
    echo "Checking every 60 seconds..."
    echo ""
    
    while true; do
        health_check
        echo ""
        sleep 60
    done
else
    health_check
fi
