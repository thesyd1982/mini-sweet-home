# ===============================
# 🌹 TMUX CONFIG - Rose Pine FAST (Same Look & Feel)
# ===============================

# Terminal settings (identical)
set default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# Speed optimizations
set -g escape-time 0
set -g repeat-time 300

# Reload config (identical)
unbind r
bind r source-file ~/.tmux.conf\; display-message "tmux.conf reloaded!" ;

# Pane resizing (identical)
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

# Synchronize panes (identical)
bind -r y setw synchronize-panes

# Mouse and vi mode (identical)
set -g mouse on
set -g display-time 0
set-window-option -g mode-keys vi

# Copy mode bindings (identical)
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection
unbind -T copy-mode-vi MouseDragEnd1Pane

# Window settings (identical)
set -g base-index 1
set -g renumber-windows on
setw -g pane-base-index 1

# Status bar position (identical)
set -g status-position top
set -gq allow-passthrough on

# Display panes colour (identical)
set -g display-panes-colour "#6e6a86"

# ===============================
# 🌹 ROSE PINE COLORS (IDENTICAL)
# ===============================

# Rose Pine Colors (exact same)
thm_bg="#191724"
thm_fg="#e0def4"
thm_cyan="#9ccfd8"
thm_black="#26233a"
thm_gray="#6e6a86"
thm_magenta="#c4a7e7"
thm_pink="#f6c177"
thm_red="#eb6f92"
thm_green="#a3be8c"
thm_yellow="#f6c177"
thm_blue="#569fba"
thm_orange="#ea9a97"
thm_black4="#555169"

# Status bar styling (identical)
set -g status-style "bg=default,fg=#e0def4"
set -g status-interval 5

# Left status (identical)
set -g status-left "#[fg=#c4a7e7,bold]#(whoami) #[fg=#555169]│ "

# Center status (identical)
set -g status-justify centre
set -g window-status-current-format "#[fg=#e0def4,bg=#26233a,bold] #I #W "
set -g window-status-format "#[fg=#6e6a86] #I #W "
set -g window-status-separator ' #[fg=#6e6a86]• '

# Status bar clean (identical)
set -g status-left ""

# Right status (identical)
set -g status-right ""

# Pane borders (identical)
set -g pane-border-style "fg=#6e6a86"
set -g pane-active-border-style "fg=#569fba"
set -g pane-border-lines single

# Message styling (identical)
set -g message-style "bg=#26233a,fg=#e0def4,bold"
set -g message-command-style "bg=#26233a,fg=#e0def4,bold"

# Mode styling (identical)
set -g mode-style "bg=#c4a7e7,fg=#191724"

# Clock (identical)
set -g clock-mode-colour "#c4a7e7"

# ===============================
# ⚡ MANUAL VIM-TMUX NAVIGATION (instead of plugin)
# ===============================

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

# ===============================
# ⚡ NO TPM/PLUGINS FOR SPEED
# ===============================
# All plugin functionality replicated manually above for speed
# - tmux-sensible: Built-in settings covered
# - vim-tmux-navigator: Manual implementation above  
# - tmux-resurrect: Removed for speed (can be added back if needed)
# - tmux-continuum: Removed for speed (can be added back if needed)
